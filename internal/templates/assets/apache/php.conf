<Directory "{{DOCUMENT_ROOT}}">
        Require all granted
        AllowOverride All
        Options -Indexes +FollowSymLinks
        DirectoryIndex index.php index.html
        AcceptPathInfo On
    </Directory>

    <FilesMatch "\.php$">
        SetHandler "{{PHP_FPM_ENDPOINT}}"
    </FilesMatch>
